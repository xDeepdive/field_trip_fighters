# Render.com deployment configuration
services:
  - type: web
    name: ftf-multiplayer-server
    env: node
    region: oregon
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
